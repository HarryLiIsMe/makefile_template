include ./Makefile.comm


LFLAGS    = -fPIC -shared 
CFLAGS    = -fPIC -shared -Wall
TARGET    = libmakefile_template 


# link
$(TARGET):$(OBJ)
 @mkdir -p $(OUTPUT)
 $(CC) $(OBJ) $(LIB_PATH) $(LIB_NAMES) $(LFLAGS) -o $(OUTPUT)/$(TARGET)_$(VERSION).so
 #@$(RM) $(OBJ)
